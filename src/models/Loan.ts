

export class Loan {
  // account: number;
  userId: number;
  reason: string;
  amount: number;

    constructor(
      // _account: number,
      _userId: number,
      _reason: string,
      _amount: number
    ) {
      // this.account = _account;
      this.userId = _userId;
      this.reason = _reason;
      this.amount = _amount;
    }
  }
